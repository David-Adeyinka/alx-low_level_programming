#include "main.h"

/**
 * _isupper - entry point
 * program checks for uppercase character.
 * @c : character to check the case
 *
 * Return:0 or 1
 */

int _isupper(int c)
{
	return (c >= 65 && c <= 90);
}
